include:
  - path: ../synapse/docker-compose.yml

services:
  bot:
    build:
      context: .
    restart: unless-stopped
    environment:
      APPSERVICE_CONFIG: config/appservice.yml
    depends_on:
      synapse:
        condition: service_healthy
    ports:
      - 9292:9292
